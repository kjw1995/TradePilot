# TradePilot 기여 규격

이 문서는 TradePilot 저장소의 브랜치, 커밋, Pull Request 작성 규칙을 정의합니다.

## 1. 기본 언어 원칙

- 커밋 제목과 본문은 한글로 작성합니다.
- PR 제목, 본문, 체크리스트와 리뷰 답변은 한글로 작성합니다.
- 아래 항목은 가독성과 도구 호환성을 위해 영문 사용을 허용합니다.
  - Conventional Commit 타입과 scope
  - 클래스, 메서드, 변수와 API 이름
  - 명령어, 파일 경로, URL과 코드
  - 라이브러리, 제품과 프로토콜의 고유명사
- 의미 없이 한글과 영문을 혼용하지 않습니다.

## 2. 브랜치 규격

브랜치 이름은 다음 형식을 사용합니다.

```text
<분류>/<영문-kebab-case-설명>
```

| 분류 | 용도 | 예시 |
| --- | --- | --- |
| `feature` | 신규 기능 | `feature/realtime-market-dashboard` |
| `fix` | 버그 수정 | `fix/sse-reconnect` |
| `refactor` | 동작 변경 없는 구조 개선 | `refactor/market-data-port` |
| `docs` | 문서 변경 | `docs/commit-pr-convention` |
| `chore` | 설정 및 유지보수 | `chore/update-gradle-wrapper` |
| `agent` | Codex가 생성한 작업 브랜치 | `agent/add-order-dashboard` |

- 영문 소문자, 숫자와 하이픈만 사용합니다.
- 하나의 브랜치는 하나의 목적만 다룹니다.
- `main`에서 직접 작업하거나 직접 푸시하지 않습니다.

## 3. 커밋 메시지 규격

### 3.1 형식

```text
<type>(<scope>): <한글 요약>

<선택 본문>

<선택 꼬리말>
```

- `scope`는 선택 사항입니다.
- 타입과 scope는 영문 소문자를 사용합니다.
- 요약은 한글 명사형으로 끝냅니다. 예: `추가`, `수정`, `분리`, `제거`, `개선`
- 요약 끝에 마침표를 사용하지 않습니다.
- 한 커밋에는 하나의 논리적 변경만 포함합니다.
- 구현 내용보다 변경 목적이 드러나도록 작성합니다.

### 3.2 타입

| 타입 | 의미 | 예시 |
| --- | --- | --- |
| `feat` | 신규 기능 | `feat(market-data): 실시간 시세 대시보드 추가` |
| `fix` | 버그 수정 | `fix(sse): 재연결 중복 실행 수정` |
| `refactor` | 동작 변경 없는 구조 개선 | `refactor(order): 주문 상태 전이 로직 분리` |
| `test` | 테스트 추가 및 수정 | `test(web): 대시보드 정적 리소스 검증 추가` |
| `docs` | 문서 변경 | `docs: 커밋 및 PR 작성 규격 정의` |
| `chore` | 일반 유지보수 | `chore: 로컬 개발 설정 정리` |
| `build` | 빌드 및 의존성 | `build: Spring Boot 버전 갱신` |
| `ci` | CI/CD 설정 | `ci: PR 테스트 워크플로 추가` |
| `perf` | 성능 개선 | `perf(market-data): 틱 저장 배치 처리 개선` |
| `style` | 포맷 등 비기능 변경 | `style: Java 코드 포맷 정리` |

### 3.3 본문과 꼬리말

본문이 필요한 경우 제목 다음에 빈 줄을 넣고 다음 내용을 한글로 설명합니다.

- 변경한 이유
- 선택한 구현 방식
- 영향 범위와 주의 사항

관련 이슈와 호환성 변경은 다음 형식을 사용합니다.

```text
Closes #123
BREAKING CHANGE: 주문 요청 필드 이름을 변경함
```

`Closes`, `BREAKING CHANGE`는 GitHub 및 Conventional Commit 호환을 위한 예외 토큰입니다.

### 3.4 금지 예시

```text
update files
수정
feat: dashboard
fix: 버그 수정 및 리팩터링 및 문서 업데이트
```

변경 대상이나 목적이 드러나지 않거나, 제목 설명이 영문이거나, 서로 다른 목적을 한 커밋에 섞은 메시지는 사용하지 않습니다.

## 4. Pull Request 규격

### 4.1 PR 제목

커밋 제목과 동일한 형식을 사용합니다.

```text
<type>(<scope>): <한글 요약>
```

예시:

```text
feat(market-data): 실시간 시세 대시보드 추가
fix(sse): 연결 해제 후 재구독 누락 수정
docs: 커밋 및 PR 작성 규격 정의
```

### 4.2 PR 상태

- 생성 시 기본 상태는 Draft입니다.
- 구현과 자체 검증이 끝난 후에만 리뷰 준비 상태로 전환합니다.
- 하나의 PR에는 하나의 기능 또는 하나의 수정 목적만 포함합니다.

### 4.3 PR 본문

PR 본문은 `.github/pull_request_template.md`를 사용하며 다음 내용을 포함합니다.

1. 개요
2. 변경 사항
3. 변경 이유
4. 영향 범위
5. 검증 결과
6. 화면 변경 사항
7. 관련 이슈
8. 체크리스트

- 단순 파일 목록 대신 사용자가 체감하는 변화와 개발 영향을 설명합니다.
- 명령어는 코드 블록 또는 인라인 코드로 작성합니다.
- UI 변경은 스크린샷 또는 영상으로 확인할 수 있게 합니다.
- 테스트를 실행하지 못했다면 이유와 미검증 범위를 명시합니다.

## 5. 리뷰 및 병합 규격

- 리뷰 의견과 답변은 한글로 작성합니다.
- 코드 식별자와 인용한 오류 메시지는 원문을 유지할 수 있습니다.
- 리뷰 요청 사항을 반영한 커밋도 이 문서의 커밋 규격을 따릅니다.
- PR의 모든 체크가 통과하고 리뷰 의견이 해결된 후 병합합니다.
- 기본 병합 방식은 Squash merge를 권장합니다.
- Squash commit 제목은 PR 제목 형식을 유지합니다.

## 6. 로컬 커밋 템플릿 적용

저장소 루트에서 다음 명령을 한 번 실행합니다.

```bash
git config commit.template .gitmessage
```

이후 `git commit`을 실행하면 이 저장소의 커밋 작성 안내가 표시됩니다.

## 7. 작성 전 확인

커밋 전:

- 변경 파일이 하나의 목적에 해당하는지 확인합니다.
- 테스트 또는 빌드 검증을 실행합니다.
- 커밋 제목의 설명이 한글인지 확인합니다.

PR 생성 전:

- 대상 브랜치와 변경 범위를 확인합니다.
- PR 제목과 본문이 한글 규격을 따르는지 확인합니다.
- 실행한 검증 명령과 결과를 기록합니다.
- UI 변경 시 화면 자료를 첨부합니다.
